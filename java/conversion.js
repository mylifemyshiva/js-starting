let score ="50abc"
console.log(typeof score)
console.log(typeof(score))

let isLoggedAn=false

let valueInNumber=Number(isLoggedAn)
console.log(typeof(valueInNumber))
/* this will change a string other then no but 
 when we see the value it will return NaN so we have to check the conversion
 "30" = 30
"30abc" = NaN
in case of conversion we have to use first letter capital line Number not number
*/

console.log(valueInNumber)
